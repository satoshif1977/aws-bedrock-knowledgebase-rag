{
  "name": "aws-bedrock-knowledgebase-rag-client",
  "version": "1.0.0",
  "description": "TypeScript client utilities for aws-bedrock-knowledgebase-rag (Python/Go 版の型安全版)",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.7.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
